---
import HeaderGallery from '../HeaderGallery/HeaderGallery.astro';
import CountdownTimer from '../CountdownTimer/CountdownTimer.tsx';
import Register from '../Register/Register.tsx';
---

<style>
  header {
    height: auto;
    background-color: var(--color-blue-one);
    position: relative;
  }
  .container {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    height: 100%;
    justify-content: space-between;
    width: 100%;
    align-items: center;
  }
  .content {
    width: 100%;
    margin-bottom: 4rem;
  }
  h1 {
    font-size: 3.5rem;
    font-weight: 700;
    color: white;
    margin-bottom: .5rem;
  }
  p {
    color: white;
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }
  .gallery {
    width: 100%;
  }

  .wave {
    position: absolute;
    height: 70px;
    width: 100%;
    background: var(--color-blue-one);
    bottom: 0;
  }

  .wave::before, .wave::after {
    content: "";
    display: block;
    position: absolute;
    border-radius: 100% 50%;
  }

  .wave::before {
    width: 55%;
    height: 109%;
    background-color: #fff;
    right: -1.5%;
    top: 60%;
  }
  .wave::after {
    width: 55%;
    height: 100%;
    background-color: var(--color-blue-one);
    left: -1.5%;
    top: 40%;
  }
  .email-copy {
    font-size: .8rem;
    margin-top: .5rem;
  }

  /* Medium only */
  @media screen and (min-width: 40em) and (max-width: 63.9375em) {
    header {
      height: 80vh;
    }
    .container {
      flex-direction: row;
    }
    .content {
      width: 50%;
    }
    .gallery {
      width: 40%;
    }
  }

  /* Large and up */
  @media screen and (min-width: 64em) {
    header {
      height: 80vh;
    }
    .container {
      flex-direction: row;
    }
    .content {
      width: 50%;
    }
    .gallery {
      width: 40%;
    }
  }

</style>

<header>
  <div class="container">
    <div class="content">
      <h1>TypeScript Day</h1>
      <p><i class="fas fa-calendar"></i> Febrero 19 2021, Evento Online.</p>
      <p>Aprende sobre el lenguage que los cambio todo para JS de la mano de profesionales, 4 speakers, 5 lighttalks, sessión de networking, Gratis!</p>
      <CountdownTimer client:visible />
      <Register client:visible />
    </div>
    <div class="gallery">
      <HeaderGallery />
    </div>
  </div>
  <div class="wave"></div>
</header>
